import { bootstrapApplication } from '@angular/platform-browser';
import { LogUploadComponent } from './app/log-upload/log-upload.component';

bootstrapApplication(LogUploadComponent).catch(err => console.error(err));
